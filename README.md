# Params-to-Object

Simple VS Code extension that logs "hello world" to the developer console when activated.

Run in VS Code:

1. Open `C:\Users\mark\apps\Params-to-Object` in VS Code.
2. Press `F5` to launch the Extension Development Host.
3. In the host window open `Help > Toggle Developer Tools` to see the log.
